# subnet

Creates a single subnet inside an existing virtual network.

## Responsibility

Only creates the subnet. Delegation and policy settings are optional inputs so the module can be reused for App Service, private endpoints, or general-purpose subnets.
