# linux-web-app

Creates a Linux Web App.

## Responsibility

The module creates only the web app. Role assignments, ACR pull permissions, private endpoints, and monitoring bindings should be composed outside the module.
