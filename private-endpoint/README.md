# private-endpoint

Creates a generic Azure Private Endpoint.

## Responsibility

Only creates the private endpoint and, optionally, its DNS zone group. The target service is provided from outside through the resource ID and subresource names.
